# Moon Patrol (Canvas)

A lightweight browser-based Moon Patrol style runner game.

## Run locally

```bash
python3 -m http.server 8000
```

Open <http://localhost:8000> and use:

- `Space` or `↑` to jump
- `R` to restart after a crash
